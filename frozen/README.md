# Frozen configuration

This directory contains historical configuration retained for reference.

Everything under `frozen/` is inactive, unsupported, and excluded from all sync and deployment flows.

Before reuse, manually review and copy only the configuration you intentionally want to reactivate.
